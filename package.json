{
  "name": "specrail",
  "private": true,
  "version": "0.1.0",
  "description": "Spec-driven orchestration service for coding-agent workflows",
  "packageManager": "pnpm@10.18.0",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "dev:api": "pnpm --filter @specrail/api dev",
    "lint": "pnpm -r lint",
    "test": "tsx --tsconfig tsconfig.base.json --test packages/core/src/domain/__tests__/*.test.ts packages/config/src/__tests__/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
